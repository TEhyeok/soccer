# 구현 로드맵 — 택틱북 (TacticBook)

| 항목 | 내용 |
|---|---|
| 문서 버전 | v1.0 |
| 작성일 | 2026-07-08 |
| 상위 문서 | [docs/PRD.md](PRD.md) |
| 작성 방식 | 6개 영역(코드 감사/v1.1/v1.2/v1.3/콘텐츠/인프라) 병렬 기획 → 완전성 비판 검토 → PM 결정 반영 종합 |

## 1. 로드맵 원칙

1. **팀 전제**: PM(사용자) + AI 개발자(Claude Code) 2인 페어. 사람 풀타임 개발자 없음 → "구현 난이도"보다 **검증 가능성과 순서**가 우선이다.
2. **기계 검증 게이트**: 트래픽이 없는 단계에서 사용자 행동 지표(체류·설치 수)를 출시 판단에 쓰지 않는다. go/no-go는 **기계 검증 가능한 항목**(테스트 green, Lighthouse, 실기기 QA 체크리스트)만으로 구성하고, 행동 지표는 "계측 가능 상태 도달"까지만 요구한다.
3. **기반은 1회만**: 배포·CI·테스트 하네스는 M0에서 단일 트랙으로 한 번 구축한다. 각 릴리스는 이를 승계·확장할 뿐 재구축하지 않는다.
4. **콘텐츠가 곧 제품**: 콘텐츠 품질 기준·검증 자동화·제작 파이프라인을 기능 개발과 동급 트랙으로 관리한다.
5. **전술 id 불변**: localStorage 즐겨찾기가 전술 id를 참조하므로, 한 번 배포된 전술 id는 절대 변경하지 않는다.
6. **스키마는 additive-only**: 기존 데이터의 재마이그레이션 없이 optional 필드 추가로만 확장한다.

## 2. 한눈에 보기

| 마일스톤 | 테마 | 기간(추정) | 핵심 산출물 |
|---|---|---|---|
| **M0** 기반 다지기 | 배포·검증·스키마 v2 | 2주 | 공개 URL, CI, 테스트 3층, 스키마 ADR, 계측+피드백 채널 |
| **v1.1** 살아 움직이는 전술 | 애니메이션 + 콘텐츠 30종 + PWA | 3~4주 | 시퀀스 재생 6종, 콘텐츠 팩 10종, 오프라인 지원 |
| **v1.2** 나만의 전술 보드 | 공유 + 커스텀 편집기 | 4주 | 이미지 공유(단톡), 드래그 편집기, 내 전술 저장 |
| **v1.3** 팀과 함께 | 백엔드·계정·팀 스페이스 | 4~6주 (착수 게이트제) | Supabase, 계정 동기화, 팀 플레이북 |
| 상시 트랙 | 릴리스 위생 | 릴리스마다 | 공지+커뮤니티 배포, 피드백 수집, 법적 체크 |

의존 관계(크리티컬 패스):

```
M0 스키마 ADR ──▶ v1.1 steps 동결 ──▶ v1.1 애니메이션 ──▶ v1.2 편집기(선수 id 재사용)
M0 배포+CI ────▶ 모든 릴리스의 검증 루프
M0 계측 ───────▶ v1.3 착수 게이트 데이터 (4주 이상 축적 필요)
v1.2 공유 ─────▶ v1.3 팀 스페이스 (공유 실사용 신호가 착수 조건)
```

## 3. 주요 아키텍처 결정 (ADR 요약)

비판 검토에서 영역 간 충돌로 드러난 사안들. M0에서 ADR 문서로 확정한다.

| # | 결정 | 선택 | 근거 |
|---|---|---|---|
| ADR-001 | **전술 보드 스키마 v2** | **선수 id 도입** (인덱스 기반 기각) | v1.2 편집기의 선수 추가/삭제가 인덱스 안정성을 깨므로 인덱스 기반 애니메이션은 v1.2에서 파산한다. 선수 id는 애니메이션 트위닝·편집기·커스텀 저장 3개 기능의 공통 기반. 20종일 때 마이그레이션하는 것이 30종·40종일 때보다 압도적으로 싸다 |
| ADR-002 | 애니메이션 데이터 구조 | `Board.steps` optional 확장, 명칭 `steps`로 통일 | 기존 단일 스냅샷 보드 = "1단계짜리 시퀀스"로 자연 해석. 기존 20종 재마이그레이션 불필요. 커스텀 보드 저장 포맷도 동일 스키마 재사용 |
| ADR-003 | 호스팅 | **Cloudflare Pages** | 무료 티어 무제한 대역폭 + PR마다 프리뷰 URL 자동 생성(PM이 코드 안 읽고 눈으로 검증하는 루프의 핵심). 해시 라우팅이라 SPA rewrite 불필요 |
| ADR-004 | 라우팅 | **해시 라우팅 유지** (v1.2까지) | History 전환은 PWA 캐시·필터 직렬화·기존 딥링크와 상호 회귀 폭탄. 검색 유입 실측 데이터가 생긴 후(v1.2 이후) 재평가 |
| ADR-005 | OG/공유 미리보기 | 대표 OG 이미지 1장 | 정적 호스팅+해시 라우팅에선 전술별 OG가 원천 불가(단일 index.html). 전술별 공유는 v1.2 이미지 내보내기로 해소 |
| ADR-006 | 애널리틱스 | 쿠키리스 경량 도구 (Plausible/GoatCounter 계열) | 무서버 제약 + 프라이버시 고지 최소화. 이벤트 사전(Event Dictionary)은 단일 문서로 관리 |
| ADR-007 | v1.3 백엔드 | **Supabase** (BaaS) | 무개발자 팀에서 자체 서버는 운영 부채. RLS 정책 테스트를 CI에 강제하는 조건부 채택 |

## 4. M0 — 기반 다지기 (2주)

> 목표: "AI가 만들고 → CI가 검증하고 → PM이 프리뷰 URL에서 확인"하는 루프 완성. 이것 없이는 이후 모든 릴리스의 품질을 사람 눈으로 전수 검증해야 한다.

### M0-E1. 검증 파이프라인 (P0)

| 스토리 | 크기 | 완료 기준(요약) |
|---|---|---|
| 린트/포맷/타입체크 표준화 | S | `npm run check` 단일 명령(eslint+prettier+tsc), 현재 코드 0 에러 통과 |
| 전술 데이터 무결성 테스트 (Vitest) | S | counters 참조 유효성(현재 오타 시 조용히 링크만 사라짐), id 유일성, 좌표 0~100 범위, 포메이션 11명 검증. 깨진 데이터 주입 시 테스트 실패 |
| 핵심 로직 단위 테스트 | M | App.tsx 인라인 필터를 순수 함수로 추출 후 조합 테스트, useHashRoute/useFavorites 테스트 |
| E2E 스모크 (Playwright) | M | PRD 사용자 스토리 1:1 매핑 5종: 카드 20개 렌더 / 검색→상세 / 즐겨찾기 새로고침 유지 / 딥링크 / 404 폴백 + 모바일 뷰포트 1종 |
| GitHub Actions CI | S | push/PR마다 check→test→build→E2E, 전체 5분 이내, README 배지 |

### M0-E2. 배포 & 릴리스 루프 (P0)

| 스토리 | 크기 | 완료 기준(요약) |
|---|---|---|
| Cloudflare Pages 프로덕션 배포 | S | 공개 URL에서 딥링크 새로고침 동작, 대표 OG 이미지+og:title 추가 |
| CI 연동 자동 배포 + PR 프리뷰 | S | main 머지 → 10분 내 프로덕션 갱신, 모든 PR에 프리뷰 URL 자동 생성 |
| 릴리스 위생 | S | footer 버전을 package.json에서 주입, CHANGELOG.md 도입 |

### M0-E3. 데이터 스키마 v2 + 콘텐츠 위생 (P0)

| 스토리 | 크기 | 완료 기준(요약) |
|---|---|---|
| 스키마 통합 ADR 확정 | S | ADR-001/002 문서화: 선수 id, Board.steps, 커스텀 보드 envelope, 명칭 통일을 한 문서에서 결정 |
| 선수·화살표 식별자 도입 + 20종 마이그레이션 | M | PlayerPos에 id, Arrow에 subjectId(optional). 스크립트로만 마이그레이션(수기 금지), 무결성 테스트 선행. 렌더 시각 회귀 0건 |
| 데이터-코드 분리 | M | 1,179줄 단일 tactics.ts를 카테고리별 분리 + 로드 시 스키마 검증. 신규 전술 1종 파일럿: 코드 수정 0줄로 추가 |
| 기존 20종 콘텐츠 감사 | M | 오탈자 수정("快속" 등), 세트피스 counters 공란 정책 확정, role 표기 일관화. 콘텐츠 스타일 가이드(docs/CONTENT_GUIDE.md) 작성 |
| 보드 렌더 스냅샷 기준선 | S | 20종 보드 스크린샷 스냅샷 확보 — v1.1 애니메이션 리팩터의 안전망 |

### M0-E4. 계측 + 피드백 채널 (P1)

| 스토리 | 크기 | 완료 기준(요약) |
|---|---|---|
| 쿠키리스 애널리틱스 도입 | S | 페이지뷰+핵심 이벤트(상세 진입, 검색, 즐겨찾기, 카운터 클릭) 대시보드 관측. 차단 시 앱 무회귀 |
| 이벤트 사전 문서 | S | PRD 성공 지표와 이벤트 매핑을 단일 문서로 (v1.1과 중복 도입 방지) |
| 런타임 에러 가시성 | S | window.onerror → track('client_error') — Sentry 없이 프로덕션 오류 인지 |
| 피드백 채널 | S | 푸터에 피드백 링크(폼/mailto) — 전술 내용 오류 제보 경로 확보 |

**M0 go/no-go**: ① main→프로덕션 10분 내 자동 배포 + PR 프리뷰 100% ② CI 5분 내 green (데이터 오류 0건) ③ 20종 스냅샷 기준선 확보 ④ 딥링크 폰 실기기 확인 ⑤ 스키마 ADR 승인 ⑥ 계측 대시보드 관측 + 피드백 채널 노출

## 5. v1.1 — 살아 움직이는 전술 (3~4주)

> 목표: 정적 화살표의 표현 한계(PRD 9장 리스크) 해소 + 콘텐츠 30종 도달 + 재방문 기반(PWA).
> **내부 순서가 중요**: steps 스키마 동결 → 스냅샷 회귀 확보 → 엔진 → 콘텐츠 팩(동결된 스키마로 제작) → 시퀀스 → PWA.

### v1.1-E1. 전술 움직임 애니메이션 (P0)

| 스토리 | 크기 | 완료 기준(요약) |
|---|---|---|
| Board.steps 스키마 확장 + 동결 | M | 선수 id 기반(ADR-001), 단계별 위치·화살표·설명 캡션. **콘텐츠 팩 착수 전 동결** — 팩 제작 중 스키마 변경 금지 |
| 재생 엔진 (순수 보간 함수 + 훅) | M | 보간 순수 함수 유닛테스트 green, `prefers-reduced-motion` 시 단계 스틸 컷 폴백 |
| 재생 컨트롤 UI | M | 재생/일시정지/단계 이동/배속, 모바일 터치 대응 |
| 애니메이션 시퀀스 6종 제작 | L | 게겐프레싱·티키타카·역습·오프사이드 트랩·니어포스트 코너·하이 프레스 (움직임 표현이 가장 아쉬운 순) |

### v1.1-E2. 콘텐츠 팩 "참조 완성" 10종 (P0, 20→30종)

| 배치 | 내용 | 크기 |
|---|---|---|
| 빌드업·전개 4종 | 3자 빌드업(살리다 라볼피아나), 인버티드 풀백, 하프 스페이스 공략, 스위칭 플레이 — 신규 카테고리 '빌드업·전개' | L |
| 공격·세트피스 3종 | 가짜 9번(False 9), 오버로드 투 아이솔레이트, 스로인 루틴 | M |
| 수비·전환 3종 | 지역방어 코너 수비, 대인방어 vs 지역방어, 수비 전환(리커버리 런) | M |

- 완료 기준: 전 항목 콘텐츠 검증 스크립트(content:check) 통과 + PM 고유명사 전수 검수 + 기존 전술과 카운터 역링크 통합.

### v1.1-E3. PWA (P1)

| 스토리 | 크기 | 완료 기준(요약) |
|---|---|---|
| 매니페스트 + 앱 아이콘 | S | Lighthouse installable 통과 (아이콘 제작은 PM 승인 필요 — 브랜드 자산) |
| 서비스 워커 오프라인 캐시 | M | 오프라인 E2E 통과. **SW kill switch 리허설 1회 실증** (캐시 사고 대비) |
| 버전 업데이트 UX | S | 새 버전 배포 시 사용자에게 새로고침 유도 토스트 |

**v1.1 go/no-go**: ① 기존 20종 스냅샷 회귀 0건 (steps 미보유 전술의 렌더가 v1.0과 동일함을 증명) ② 신규 10종 content:check 100% + PM 검수 완료 ③ 시퀀스 6종 reduced-motion 폴백 + 보간 유닛테스트 green ④ PWA installable + 오프라인 E2E + kill switch 리허설 ⑤ 애널리틱스 차단 시 무회귀 ⑥ JS gzip 90KB 이하, Lighthouse 모바일 Perf 90+/A11y 95+

## 6. v1.2 — 나만의 전술 보드 (4주)

> 목표: 페르소나 1순위 시나리오 "우리 팀 전술을 만들어 단톡에 공유"의 완성.
> **공유를 편집기보다 먼저** 구현한다 — SVG→PNG 변환과 Web Share API가 이 릴리스의 플랫폼 리스크(iOS Safari/카카오톡 인앱)이므로 조기에 소거한다.

### v1.2-E1. 전술 이미지 공유 (P0 — 선행)

| 스토리 | 크기 | 완료 기준(요약) |
|---|---|---|
| 보드 PNG 렌더링 파이프라인 | M | SVG→canvas→PNG, 워터마크(앱 이름+URL), 라이브러리 전술에서 동작 |
| '이미지로 공유' 버튼 | M | Web Share API + 미지원 환경 다운로드 폴백. iOS Safari·Android Chrome·카카오톡 인앱 3환경 실기기 검증 |

### v1.2-E2. 커스텀 보드 편집기 코어 (P0)

| 스토리 | 크기 | 완료 기준(요약) |
|---|---|---|
| 편집 화면 뼈대 + 선수 드래그 | M | 좌표 역변환(화면→논리)은 M0에서 분리한 px() 유틸 재사용. 드래그 vs 페이지 스크롤 충돌 해결(터치 시 스크롤 잠금) |
| 선수·상대·공 팔레트 | M | 추가/삭제/라벨 편집 — 선수 id 자동 부여(ADR-001 재사용) |
| 화살표 그리기 (run/pass/press) | L | 터치로 시작점→끝점 드래그, 곡률 조정, 삭제 |
| Undo·전체 지우기·모바일 편집 QA | M | 실기기 QA 체크리스트 통과, 의도치 않은 스크롤 0건 |

### v1.2-E3. 내 전술 저장 & 관리 (P0)

| 스토리 | 크기 | 완료 기준(요약) |
|---|---|---|
| 커스텀 전술 스키마 v1 + 버저닝 | M | envelope `{version, items}`, 마이그레이션 프레임, 손상 데이터 단위테스트. **steps 보유 템플릿을 열 때의 동작(보존/제거) 정의 포함** |
| 저장·이름 짓기·자동 임시저장 | M | 강제 새로고침 후 draft 복원 |
| '내 전술' 목록·관리 + 공유 연결 | M | 목록/삭제/복제, E1 이미지 공유와 연결 |
| **JSON 내보내기/가져오기** | S | localStorage 유실 대비 사용자 백업 + v1.3 계정 마이그레이션 보험 |

### v1.2-E4. 라이브러리 템플릿으로 시작 (P1)

| 스토리 | 크기 | 완료 기준(요약) |
|---|---|---|
| 상세 → "이 전술로 보드 만들기" | S | 라이브러리 보드가 편집기에 복사되어 열림 |
| 편집기 시작 화면 | M | 빈 보드 vs 포메이션 템플릿 선택 |
| 출처 표시 | S | 템플릿 기반 커스텀 전술에 원본 링크 |

**v1.2 go/no-go**: ① 이미지 공유(또는 폴백) 3환경 실기기 성공률 100% ② 초견 테스터 5인 중 4인이 "11명 배치+화살표 3개"를 3분 내 완성 (테스터 모집은 상시 트랙에서 선행) ③ 편집 중 의도치 않은 스크롤 0건 ④ 저장 마이그레이션·손상 데이터 테스트 100% + draft 복원 통과 ⑤ 기존 라이브러리 스냅샷 회귀 0건 ⑥ JSON 내보내기 제공

## 7. v1.3 — 팀과 함께 (4~6주, 착수 게이트제)

> **착수 게이트 (출시 게이트와 별도)**: ① 애널리틱스 4주 이상 데이터 축적 ② v1.2 공유 기능 실사용 신호 존재 ③ 베타 팀 3곳 모집 확정. 미충족 시 착수 보류하고 원인(콘텐츠 밀도 vs 진입 장벽)을 먼저 분석한다.

### 에픽 구성 (순서 = 크리티컬 패스)

| 에픽 | 내용 | 크기 |
|---|---|---|
| E1. 백엔드 파운데이션 | Supabase 채택 ADR, 스키마 설계, 환경 분리, **RLS 정책 + 자동 정책 테스트 CI 강제**. 데이터 접근은 모듈 1개로 모으는 수준의 얇은 추상화(과설계 금지) | M×3 |
| E2. 계정·동기화 (익명 우선) | 매직링크+Google OAuth, 즐겨찾기 클라우드 동기화(localStorage 병합 마이그레이션, 유실 0건), 계정 삭제·개인정보처리방침(국내 개인정보보호법 대응) | M×2 + S |
| E3. 팀 스페이스 | 팀 생성·초대, 팀 플레이북(전술 큐레이션+팀 노트), 읽기 전용 공유 링크(비로그인 열람), 커스텀 전술 연동 | M×3 + L |

### v1.3에서 분리·연기된 것

- **다국어(영어)**: 착수 게이트(비한국어 트래픽 10%) 충족 시 **별도 릴리스**로 분리. SEO 부재 상태의 번역은 사용자 획득으로 이어지지 않음.
- **수익화**: 지불의사 프록시 지표 계측 설계만 v1.3에 포함. 결정(ADR)은 팀 스페이스 사후 검증 데이터 이후.

**v1.3 go/no-go**: ① RLS 정책 테스트(허용+거부 각 1건 이상) CI 강제 ② 비로그인 경험 무회귀(기존 E2E 100%) ③ 즐겨찾기 병합 유실 0건 ④ 매직링크 네이버·카카오·지메일 실계정 딜리버리 확인 ⑤ 공유 링크 비로그인 열람 E2E + 토큰 재발급 검증 ⑥ 개인정보처리방침·계정 삭제 완비

**v1.3 사후 검증 (출시 4주 후)**: 팀당 실사용 멤버 중앙값 3명 이상 + 공유 링크 열람 수 > 발급 수. 미달 시 팀 기능 고도화를 **중단**하고 원인 분석 선행 (사전 합의된 중단 조건).

## 8. 상시 트랙 — 릴리스 위생

모든 릴리스에 반복 편입되는 고정 태스크. 트래픽 확보 활동 없이는 데이터 기반 게이트 전부가 공전한다.

| 태스크 | 시점 |
|---|---|
| 릴리스 공지 + 커뮤니티 배포 (축구 동호회·전술 커뮤니티) | 릴리스마다 |
| 피드백 수집·분류 (전술 오류 제보 최우선 처리) | 상시 |
| 법적 체크 1항목 | M0: 오픈소스 고지 / v1.1: famousTeams 실존 구단명 고지 검토 / v1.2: 유소년 드릴 안전 면책 / v1.3: 개인정보처리방침·이용약관 |
| 사용성 테스터 풀 모집 (v1.2 게이트에 5인 필요) | v1.1 릴리스 직후부터 |
| 지원 브라우저·기기 매트릭스 문서 유지 (iOS Safari 최소 버전, 카카오톡 인앱 정책) | M0 정의, 이후 갱신 |

## 9. 백로그 (이번 로드맵에서 컷 — 근거 포함)

| 항목 | 컷 근거 | 재검토 조건 |
|---|---|---|
| 온보딩 코치마크 | 전술 20~30종 규모 앱에 과함 | 콘텐츠 50종+ 도달 시 |
| 전술 비교 뷰 | 기존 콘텐츠가 대비형 서술이 아님 | 콘텐츠 리라이트와 함께 |
| History 라우팅 전환 + 프리렌더 + sitemap | 검색 유입 실측 전의 선제 최적화, v1.1 과부하 주범 | v1.2 이후 애널리틱스 데이터로 재평가 |
| PWA 설치 유도 배너 | 초기 트래픽에서 효과 측정 불가 + 이탈 요인 | 주간 방문 500+ 도달 시 |
| 콘텐츠 검수 모드 화면(#/review) | PR 프리뷰 + 스냅샷 회귀가 동일 역할 | 콘텐츠 기여자가 늘어나면 |
| 역할 도감·상황 전술 12종 (40→52종) | v1.3 백엔드와 동시 수행은 2인 팀에 물리적 불가 | v1.3 이후 별도 콘텐츠 릴리스 |
| 영상 링크 큐레이션 | 링크 수동 점검은 반복 운영 부채 | CI 링크 체커 자동화 가능 시 |
| 행동 지표 목표치 (설치 월 10건 등) | 트래픽 부족 상태에서 노이즈 | 주간 방문 500 도달 후 설정 |

## 10. 리스크 톱 5

| 리스크 | 대응 |
|---|---|
| 스키마 ADR을 건너뛰고 기능부터 개발 → v1.1/v1.2 스키마 2~3벌 재작업 | M0 go/no-go에 ADR 승인 포함 (게이트로 강제) |
| 콘텐츠 사실 오류 — PM 전술 지식의 한계 | 피드백 채널 최우선 처리 + 콘텐츠 스타일 가이드의 출처 규칙 + 커뮤니티 베타 리뷰어 모집(상시 트랙) |
| 모바일 실기기 회귀 — "데스크톱 통과 ≠ 실기기 통과" (PWA·터치 편집기) | 릴리스 게이트에 실기기 체크리스트 명시, 기기 매트릭스 문서 유지 |
| E2E가 느리거나 불안정 → CI 무시 습관 | 스모크 5종 상한 + CI 5분 상한을 게이트로 유지 |
| v1.3 착수 게이트 미충족 (트래픽·베타 팀 부재) | 사전 합의된 보류 조건 — 팀 기능 대신 콘텐츠·공유 고도화로 선회 |

## 11. 지금 바로 할 일 (M0 착수 순서)

1. ADR-001~007 문서 작성 및 승인 (스키마 결정이 전부의 선행 조건)
2. 검증 파이프라인: check 스크립트 → 데이터 무결성 테스트 → E2E 스모크 → CI
3. Cloudflare Pages 배포 + PR 프리뷰 (도메인 구입은 PM 수동 작업 — 병행)
4. 스키마 v2 마이그레이션 (테스트 선행 후 스크립트로)
5. 콘텐츠 감사 + 스타일 가이드
6. 계측 + 피드백 채널 → **M0 go/no-go 리뷰**
